# Firebase Admin SDK Service Account Key (JSON string — paste the full JSON from Firebase Console)
# Or base64-encode it and paste as a single line
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}

PORT=3000
